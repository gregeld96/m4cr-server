export enum AccountListFilterSort {
  FIRST_NAME = 'firstName',
  FIRST_NAME_DESC = '-firstName',
  EMAIL = 'email',
  EMAIL_DESC = '-email',
  CREATED_AT = 'createdAt',
  CREATED_AT_DESC = '-createdAt',
}

export enum ContentListFilterSort {
  TITLE = 'title',
  TITLE_DESC = '-title',
  STATUS = 'status',
  STATUS_DESC = '-status',
  CREATED_AT = 'createdAt',
  CREATED_AT_DESC = '-createdAt',
}

export enum FeedbackListFilterSort {
  CREATED_AT = 'createdAt',
  CREATED_AT_DESC = '-createdAt',
}

export enum AddressListFilterSort {
  ID = 'id',
  ID_DESC = '-id',
}